<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body style="font-size:15px; padding:30px;">
  <%-include('partials/_header.ejs') %>
  <table>
    <% for (var url in urls) { %>
      <tr>
        <td>
          <%= url %>
        </td>
        <td>
          <br>
          <%= urls[url].longURL %>
          <form method="POST" action="/urls/<%=url%>/delete">
            <button type="submit">🗑️</button>
          </form>
            <form method="GET" action="/urls/<%=url%>/">
            <button type="submit">Edit</button>
          </form>
        </td>
      </tr>
    <% } %>
  </table>
  <br>
  <a href="urls/new">Create new short url</a>

</body>
</html>